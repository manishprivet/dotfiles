[user]
	email = manishprivet@protonmail.com
	name = Manish Kumar
[pull]
	rebase = false
[alias]
        amend = commit -s --amend
        cm = commit -s -m
        commit = commit -s
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
